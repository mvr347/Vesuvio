# Dependencies for Vesuvio's automatic ONNX model retraining (train_models.py).
# Install once on the server host with: pip install -r requirements.txt
numpy>=1.24
scikit-learn>=1.3
skl2onnx>=1.16
onnx>=1.15
